---
# ApplicationSet: local ingresses (mgmt cluster)
# Matches argocd/applications/mgmt/*/app.yaml
# Only generates apps when ingress.hostname is defined (using matrix + elementsYaml filter)
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: mgmt-ingress
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=zero"]
  generators:
    - matrix:
        generators:
          - git:
              repoURL: git@github.com:IronPhysique/k8-devops.git
              revision: main
              files:
                - path: "argocd/applications/mgmt/*/app.yaml"
          - list:
              elements: []
              elementsYaml: |
                {{ if index .ingress "hostname" }}
                - dummy: "true"
                {{ end }}
  template:
    metadata:
      name: 'ingress-{{ .path.basename }}'
    spec:
      project: mgmt-platform
      source:
        repoURL: git@github.com:IronPhysique/k8-devops.git
        targetRevision: main
        path: argocd/applicationsets/templates/ingress
        helm:
          releaseName: '{{ .path.basename }}'
          valuesObject:
            hostname: '{{ index .ingress "hostname" }}'
            serviceName: '{{ index .ingress "serviceName" | default .path.basename }}'
            namespace: '{{ index .ingress "namespace" | default .path.basename }}'
            port: '{{ index .ingress "port" | default "80" }}'
            # Target IP for ExternalDNS (mgmt cluster Traefik)
            targetIP: '{{ index .ingress "targetIP" | default "192.168.178.144" }}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{ index .ingress "namespace" | default .path.basename }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
