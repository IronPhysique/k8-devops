# Alloy application configuration
app:
  name: alloy
  namespace: monitoring
  cluster: mgmt
  project: mgmt-platform

  # Helm chart configuration
  source:
    path: argocd/applications/mgmt/platform/alloy/charts
    helm:
      releaseName: alloy
      valueFiles:
        - ../config/values.yaml

  destination:
    server: https://kubernetes.default.svc

  # Sync policy
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 2m

# Ingress configuration (used by app-routes)
ingress:
  enabled: false
  # hostname: alloy.iron-lab.org
  # namespace: monitoring
  # port: 80
