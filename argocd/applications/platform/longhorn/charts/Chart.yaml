apiVersion: v2
name: longhorn
description: Helm chart wrapper for Longhorn via ArgoCD
version: 0.1.0
dependencies:
  - name: longhorn
    version: "1.12.0"
    repository: "https://charts.longhorn.io"
