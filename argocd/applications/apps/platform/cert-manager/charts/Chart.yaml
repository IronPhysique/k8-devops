apiVersion: v2
name: cert-manager
description: Helm chart wrapper for cert-manager via ArgoCD (apps cluster)
version: 0.1.0
dependencies:
  - name: cert-manager
    version: "v1.16.2"
    repository: "https://charts.jetstack.io"
